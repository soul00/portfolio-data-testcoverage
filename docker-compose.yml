services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: gate
      POSTGRES_PASSWORD: gate
      POSTGRES_DB: gate
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gate"]
      interval: 2s
      timeout: 2s
      retries: 15
